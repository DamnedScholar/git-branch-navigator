'use babel';

import GitBranchNavigatorView from '../lib/git-branch-navigator-view';

describe('GitBranchNavigatorView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
